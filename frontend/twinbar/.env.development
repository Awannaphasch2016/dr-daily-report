# Development environment configuration
# Vite exposes environment variables prefixed with VITE_

# Local development API (FastAPI running on localhost)
VITE_API_BASE_URL=http://localhost:8001

# Static API URL (CloudFront CDN for precomputed JSON data)
# In development, falls back to VITE_API_BASE_URL
VITE_STATIC_API_URL=
